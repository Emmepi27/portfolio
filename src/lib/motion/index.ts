/**
 * Motion foundation — costanti e query (senza GSAP).
 * Preset con GSAP: importare da `@/lib/motion/presets` solo in file `"use client"`.
 */
export { MOTION } from "./constants";
export { MOTION_QUERIES } from "./queries";
